import React from 'react';
import {Text} from 'react-native'

Profile.propTypes = {
    
};

function Profile(props) {
    return (
        <>
          <Text>Profile</Text>
        </>
    );
}

export default Profile;